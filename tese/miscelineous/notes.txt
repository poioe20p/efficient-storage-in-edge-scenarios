# Start ssh-agent (if not running)
Get-Service ssh-agent | Set-Service -StartupType Automatic
Start-Service ssh-agent

# Add your key (you'll type the passphrase once per boot)
ssh-add ~/.ssh/edge-testop