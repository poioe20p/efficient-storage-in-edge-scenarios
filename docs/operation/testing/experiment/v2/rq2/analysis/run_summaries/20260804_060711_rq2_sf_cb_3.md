# Run Summary - 20260804_060711

## Conclusion
Mis-aligned `fixed_storage_first` × compute-bound: compute fires were suppressed by policy (selected=none), no compute scale-out occurred (server_count 1.0 throughout), and the single storage action relieved nothing (0/1) — H2 wrong-action cell; no scale-down (expected when the allowed tier adds only 1 node).

## Main Points
- **Bottleneck validation (G2): PASS both LANs** — compute-bound induced correctly: median `proc_ms` 0.74 / 0.58 ms vs `db_ms` 0.0 ms; CPU 56.0 / 65.9 %; storage CPU 0.0 %.
- **Actions: compute=0, storage=1** — fixed-arm suppression (criterion 3) holds: storage-first never emits compute; the classifier flagged compute in 54/78 (69.2 %) episode windows but the gate turned those into `selected="none"` (counterfactual analyzable).
- **Budget (criterion 5):** storage 1/4 per LAN (lan2 0) — budget mostly unused; the mis-alignment cost here is *foregone compute relief*, not budget exhaustion.
- **Relief (criterion 6):** 0/1 recovered in the targeted (storage) tier — no relief; compute (the actual bottleneck) never acted on.
- **Episode quality:** p50 0.160 s, p95 0.376 s, p99 0.876 s; failures 235/99,881 = 0.24 % (LAN1 0.22 %, LAN2 0.25 %). Notably NOT degraded — this replicate's 1-server compute tolerated the episode.
- **TTFT:** storage 39.1 s (single LAN1 spawn); TFR n/a for storage.
- **Efficiency (criterion 9):** storage node-minutes 8.00 (0.079/1000 req) — small wasted spend; compute 0.00.
- **Scale-down (criterion 7): NOT occurred** — no removals; expected per plan for the mis-aligned cell whose allowed tier added a single node (absence reported, not failed). T9.8 OK.

## Evidence
### Elasticity Events
- 1 dynamic spawn: `edge_storage_lan1_dyn1` at 06:19:32, online 06:19:33, `rs_secondary_ready` 06:20:05 (~33 s). No dynamic compute containers — `container_events.csv` shows zero `edge_server_*_dyn*`.
- No `node_removing` / `cleanup_done` events — scale-down never armed (allowed tier barely scaled).
- No controller errors / no restart mid-run (campaign context, C2).
- Scale-down: **no** (expected for `sf×cb`).

### Tier 1 Selective Sync
- **Disabled (not exercised in RQ2)** — `SS_ENABLED=0`; no `sel_sync_*` containers.

### Resource Shape
- `resource_summary.csv` — episode: `server_count` mean 1.0 (max 1.0) — no compute scale-out despite CPU 56–66 %; `storage_count` mean 0.175 (max 1.0) — one storage node, never serving a pressured data tier (T_db = 0). Demand_drop: storage mean 1.48 (static+dynamic storage remain).
- Bottleneck-validation signals: `T_proc` 0.74 / 0.58 ms, CPU 56.0 / 65.9 %, `T_db` 0.0 ms, storage CPU 0.0 % → purely compute/CPU pressure, which the policy cannot act on.

### Request Latency by Phase
| Phase | Mean (ms) | Median (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: | ---: |
| baseline | 226 | 6 | 1083 | 0 / 237 (0.00 %) |
| compute_bound_episode | 235 | 160 | 376 | 235 / 99,881 (0.24 %) |
| recovery_gap | 312 | 6 | 1120 | 0 / 121 (0.00 %) |
| demand_drop | 237 | 6 | 1081 | 1 / 1,625 (0.06 %) |

### Traffic Handling
- Episode phase dominates: 99,881 / 101,864 requests (98 %). p99 0.88 s — 238 episode requests ≥ 29.9 s (0.24 % timeout tail), small.
- Per-LAN split symmetric (0.22 % / 0.25 %). Single compute server handled the full episode at 8 rps/client without collapsing — the induced compute pressure was CPU-elevation without latency breach in this replicate.

## Practical Interpretation
- **H2 alignment (partial):** the wrong fixed arm demonstrably did *not* relieve the compute tier — compute never scaled (server_count 1.0), the storage action relieved nothing (0/1). The plan-predicted "longer degradation / wasted budget" materializes here as **foregone relief + small wasted storage spend (8.0 node-minutes)**, not as elevated latency/failures, because 1 compute server tolerated the workload in this replicate.
- **Criterion 8 caveat (cross-over):** in this `cb` replicate, `sf` is worse on p50/p95 latency than `cf`/`ba` (p50 0.160 s vs ~0.003 s; p95 0.376 s vs 0.234/0.237 s) but NOT worse on failure rate (0.24 % vs 0.53 %/0.15 %) — the cross-over on latency still holds; the resource-shape evidence (no compute scale-out) confirms the mis-alignment.
- **Criteria:** C1 present; C2 PASS; C3 suppression holds (compute fires logged, selected=none); C5 budget 1/4 (not binding); C6 no relief; C7 no scale-down (expected); C9 low wasted node-minutes; C10 no restart.

## Follow-Up
- None (replicate-level behavior; cross-over assessed at cell level in the campaign comparison).
