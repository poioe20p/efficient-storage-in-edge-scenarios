# Run Summary - 20260804_070226

## Conclusion
Mis-aligned `fixed_compute_first` × data-bound: no storage signal path (storage fires suppressed → selected=none), so the data tier was never relieved — storage_count stayed ~1, episode failure rate was the worst of the 6 runs (3.38 %, LAN2 5.44 %), p99 pinned at the 30 s timeout — the clearest H2 "stays degraded" cell in this block.

## Main Points
- **Bottleneck validation (G2): PASS both LANs** — data-bound induced correctly: median `db_ms` 307.1 / 305.5 ms vs `proc_ms` 58.4 / 88.9 ms; storage CPU 35.9 / 54.7 %.
- **Actions: compute=8, storage=0** — fixed-arm suppression (criterion 3) holds: compute-first never emits storage; the classifier flagged data (storage) in 31/49 (63.3 %) episode windows but the gate turned those into `selected="none"` — the suppressed tier's fire is logged, counterfactual analyzable.
- **Budget (criterion 5):** compute 4/4 per LAN = cap — budget fully **wasted on the wrong tier** (H2). T9.8 fire-keyed scale-down OK.
- **Relief (criterion 6):** 4/8 "recovered" but in the **compute** tier — compute score falls because compute is not the bottleneck; the storage (data) tier is never acted on → no data relief, service stays degraded (H2).
- **Episode quality:** p50 0.495 s, p95 1.990 s, p99 30.0 s (timeout cap); failures 993/29,399 = **3.38 %** (LAN1 1.16 %, LAN2 5.44 %). Worst failures of the block.
- **TTFT:** compute median 39.25 s; TFR median 35.9 s — compute capacity arrives fast but buys nothing for the data bottleneck. Negative `init_time_s` (−1.3 to −7.2) is the known tooling artifact.
- **Efficiency (criterion 9):** compute node-minutes 131.10 and **4.187/1000 req — highest per-demand compute cost of the 6 runs** (wasted actions); storage 0.00.
- **Scale-down (criterion 7): occurred** — 8 compute removals all DONE (mis-aligned cells may still scale down their allowed tier after it scaled up; presence is correct, not failed).

## Evidence
### Elasticity Events
- 8 dynamic compute spawns (4/LAN) in `data_bound_episode` (07:03:53–07:07:51), online ~1 s after spawn. No dynamic storage (`storage_count` stays ~1).
- Scale-down: 8 `node_removing` + 8 `cleanup_done` (last event 07:18:00, demand_drop); all DONE, 0 FAILED.
- No controller errors / no restart mid-run (campaign context, C2).
- Scale-down: **yes** (allowed tier scaled up, then reclaimed).

### Tier 1 Selective Sync
- **Disabled (not exercised in RQ2)** — `SS_ENABLED=0`; no `sel_sync_*` containers.

### Resource Shape
- `resource_summary.csv` — episode: `server_count` mean 1.58 (max 3.0) — compute scaled out; `storage_count` mean 0.90 (max 1.0) — **data tier never scaled**. Demand_drop: server mean 1.05.
- Bottleneck-validation signals: `T_db` 307.1 / 305.5 ms dominates `T_proc` 58.4 / 88.9 ms, storage CPU 35.9 / 54.7 % (> floor 35) → storage is the pressured tier the policy cannot act on.

### Request Latency by Phase
| Phase | Mean (ms) | Median (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: | ---: |
| baseline | 256 | 6 | 1109 | 0 / 233 (0.00 %) |
| data_bound_episode | 953 | 495 | 1990 | 993 / 29,399 (3.38 %) |
| recovery_gap | 601 | 6 | 1247 | 1 / 106 (0.94 %) |
| demand_drop | 296 | 7 | 1085 | 3 / 1,571 (0.19 %) |

### Traffic Handling
- Episode phase dominates: 29,399 / 31,309 requests (94 %). p99 = 30.0 s (CURL_MAX_TIME ceiling) → 317 episode requests ≥ 29.9 s (1.1 % timeouts), the largest timeout fraction of the block.
- **Per-LAN asymmetry:** LAN2 failures 5.44 % vs LAN1 1.16 % — LAN2's storage (also 1 node, no scale-out) bore worse data pressure; both LANs show the mis-aligned signature (storage ~1 node, failures elevated).

## Practical Interpretation
- **H2 alignment (strong):** this is the plan's "no signal path to storage → stays degraded" cell. The data tier never scaled (storage_count ~1), episode failures 3.38 % and p99 at the timeout cap, and the full compute budget (4/4 per LAN) was spent on a tier that is not the bottleneck — highest compute node-minutes per demand (4.187/1000 req) of the block.
- **Criterion 8 (cross-over):** against the aligned `db` cells, `cf` is clearly worse than `sf_db_3` (1.05 %) and `ba_db_3` (0.79 %) on failures — the headline cross-over direction holds for this block.
- **Criteria:** C1 present; C2 PASS; C3 suppression holds (storage fires logged, selected=none); C5 budget binds (wrongly); C6 no data relief (compute "recovery" 4/8 is not bottleneck relief — flag for the relief graph interpretation); C7 scale-down present; C9 worst efficiency; C10 no restart.
- **Divergence / note:** the relief tool's "4/8 recovered" for compute actions must NOT be read as service relief — compute score recovers while the service stays degraded; the relief graph needs the targeted-tier lens (storage never targeted).

## Follow-Up
- None (this cell is the H2 contrast arm; cross-over assessed at cell level in campaign comparison).
