# Run Summary - 20260804_005431

## Conclusion

Mis-aligned arm (fixed_storage_first × compute-bound): zero compute scale-out, a single storage action that could not help compute pressure, and ~1/3 less completed demand than the aligned arms. Degradation was moderate (p95 0.37 s, p99 0.66 s, 0.26 % failures) rather than catastrophic — the H2 "stays degraded" cost shows up here as **throughput loss + no compute relief** rather than timeouts.

## Main Points

- Episode induction valid: BV PASS both LANs — `proc_ms` 0.69 / 0.73 ms, `db_ms` 0.0 (compute-bound confirmed); compute CPU 19 % / 17 %.
- Fixed-arm suppression (criterion 3): **0 compute, 1 storage** action. Storage rarely fired (no storage pressure), so the budget was *idle* — this is the "no-action" form of misalignment (contrast cf_db_1, which exhausted its budget on the wrong tier).
- No compute relief anywhere: `server_count` pinned at 1.0 through the episode (max 1.0); relief 0/1 in-tier.
- Cost of the wrong policy (H2): episode completed only **98,601 requests** vs 145,308 (cf_cb_1) / 154,696 (ba_cb_1); episode p50 0.163 s — ~24× the aligned cf_cb_1 (0.0068 s); p95/p99 elevated (0.370 s / 0.664 s vs 0.254 s / 0.503 s).
- The single storage spawn was **not observed removed** in `elasticity_events.csv` (no `node_removing`/`cleanup_done` for it) — see Follow-Up.

## Evidence

### Elasticity Events

- Only 1 dynamic container: `edge_storage_lan2_dyn1` (storage, lan2), spawned 01:07 during the episode; no dynamic compute.
- No scale-down observed for that node in the run window (removal may post-date the sampled window — limitation).
- Time-to-usable-capacity (storage): TTFT **39.4 s** for the single spawn; no compute spawns.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `compute_bound_episode`: `server_count` mean 1.0, max 1.0 (**no compute scale-out**); `storage_count` mean 0.17, max 1.0.
- `demand_drop`: storage mean 1.44, max 2.0 — the lone dynamic storage node persisted into demand_drop; compute stayed 1.0.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 382 | 1185 | 1/234 (0.43 %) |
| compute_bound_episode | 238 | 370 | 256/98,601 (0.26 %) |
| recovery_gap | 297 | 1094 | 0/120 (0.00 %) |
| demand_drop | 308 | 1085 | 4/1,543 (0.26 %) |

### Traffic Handling

- Episode phase dominant but **under-completed**: 98,601 vs ~145–155 k in the aligned/ba cells — the static 1-server/LAN capacity throttled demand (fewer requests recorded, client-side concurrency limited).
- Per-LAN episode failures balanced: lan1 0.25 %, lan2 0.27 % (low absolute failures — degradation is latency/throughput, not errors).
- No timeout-bound episode: p99 0.66 s, max 30.0 s (single tail). Node-minutes: compute 0.00, storage 7.84 (0.078/1,000).

## Practical Interpretation

- **H2 reproduced in its "no-action" form:** the mis-aligned storage-first arm structurally cannot add compute, and storage does not fire under compute pressure — the policy's actions (when any) land on a tier with no pressure. The cost is throughput loss and elevated p50/p95/p99 versus cf/ba, not a failure collapse.
- **Divergence from plan wording:** the plan frames sf×cb as "wastes budget on storage"; in this run the budget is largely *unused* (1/4 actions) rather than exhausted — the waste is the lost opportunity on compute, which the plan's H2 mechanism still captures.
- Criterion 8: cf_cb_1 ≈ ba_cb_1 clearly beat sf_cb_1 on completed demand and p50; latency gap at p95/p99 is real but moderate.

## Follow-Up

Confirm whether the single dynamic storage node is eventually reclaimed after the sampled window (check container_events tail / next run's baseline) — a dangling storage container would be a cleanup-defect flag for the sf_cb cell.
