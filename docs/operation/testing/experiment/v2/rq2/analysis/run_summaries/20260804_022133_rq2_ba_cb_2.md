# Run Summary - 20260804_022133

## Conclusion

`bottleneck_aware` under a compute-bound episode classified the episode as compute-bound, scaled the compute tier to the 4/LAN action-budget cap, and achieved relief with low episode latency/failures — H1 supported. The 2 residual storage fires (recovery_gap/demand_drop) are the accepted latency-tail behavior, not a misclassification of the episode.

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_proc 0.44 / 0.40 ms vs T_db 0.00 ms) — the episode is genuinely compute-bound (criterion 2).
- 10 actions: **8 compute + 2 storage**. Compute budget at cap (lan1 c=4, lan2 c=4); storage 1/LAN. No `reason="budget_exhausted"` row — no 5th fire occurred, so budget binds by usage==cap (criterion 5).
- Classifier-vs-episode agreement **31/49 (63.3%)** over episode windows where both tiers were eligible — above chance (H3).
- **T9.8 fire-keyed scale-down OK**; compute scale-down ran — 3 removals already during the episode (02:28–02:31), the rest in demand_drop, all cooldown-protected — criterion 7 met (divergence from the plan's recovery_gap/demand_drop phrasing: reclaims began earlier).
- Relief: 2/10 actions recovered in-tier, median recovery 40.9 s (criterion 6; sustained-pressure episode means most recoveries fall in recovery_gap/demand_drop).
- Episode service quality: mean 90 ms, p50 3.7 ms, p95 244 ms, p99 483 ms; 966 failures / 151 298 requests (0.64%).
- Node-minutes: compute **130.41**, storage **15.10** (0.851 / 0.099 per 1000 recorded requests; 153 264 recorded).
- TTFT median: compute 29.3 s, storage 39.7 s; TFR compute 21.1 s.

## Evidence

### Elasticity Events

- 8 dynamic compute containers (4/LAN, `edge_server_*_dyn1..4`) spawned during the episode; each add→ready ≈1 s via `vip_backend_registered` (scale-up anchors: spawn → online → VIP-registered).
- 2 dynamic storage containers (`edge_storage_*_dyn5`) spawned in recovery_gap (lan1) / demand_drop (lan2); ready via `rs_secondary_ready` after ≈31 s — residual compute-bound storage fires.
- Scale-down (criterion 7): compute removals 02:28–02:37 — `lan1_dyn1`, `lan1_dyn2`, `lan2_dyn4` removed **during the episode** as the median compute signal recovered after scale-up, 5 in demand_drop; all `DONE` with clean `net_cleanup_s`, cooldown-respecting (T9.8 OK). Storage dyn5 remained up at end of run (idle).
- No controller errors/alerts in the parsed elasticity events; no controller restart mid-run (C2).
- `rq2_spawn_metrics.csv`: compute TTFT 19–50 s, TFR 12–44 s; storage TTFT ≈40 s. Negative `init_time_s` is the known tooling artifact (TTFT anchors on `per_node_stats` window granularity lagging HTTP TFR) — not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `server_count` mean 1.93 / max 4.0 (compute scaled 1→4/LAN); `storage_count` mean 0.17 / max 1.0 (no storage scale-out during the episode).
- Bottleneck signals (episode): compute CPU 18.97 % (lan1) / 62.02 % (lan2), T_proc 0.44/0.40 ms vs T_db 0.00 ms, storage CPU 7.73 % / 0.00 % — compute pressure dominates; the storage signal stayed below the CPU floor 35 trigger.
- Env knobs: `SCALEUP_POLICY=bottleneck_aware`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 371 | 1100 | 1 (0.43%) |
| compute_bound_episode | 90 | 244 | 966 (0.64%) |
| recovery_gap | 569 | 1103 | 1 (0.94%) |
| demand_drop | 257 | 1087 | 2 (0.12%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 0.64% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (151 298 of 153 264 requests, 100% `service_pressure` mix at 8 req/s/client). Per-LAN episode failures: lan1 1.10%, lan2 0.27% — lan1 slightly higher, both well under 2%.
- No 30 s timeout tail in the episode (p99 483 ms); baseline/recovery/demand_drop all healthy.

## Practical Interpretation

- **H1 (right action):** supported — `bottleneck_aware` selected compute (the pressured tier) and reached parity with the correctly-aligned `cf` arm's service quality (p50 3.7 ms, p95 244 ms, 0.64% failures), while the mis-aligned `sf` arm stays degraded (this run is replicate 2 of `ba_cb`).
- **H2 (wrong-action cost):** the 2 late storage fires (1/LAN) spent budget on the wrong tier but produced only 15.10 storage node-minutes — small, accepted cost; no targeted-tier relief expected and none credited (storage was not the bottleneck).
- **H3 (decision quality):** 63.3% agreement on episode windows, above chance.
- Criteria: 2 ✅, 4 ✅, 5 ✅ (cap reached), 6 ⚠️ (2/10 in-tier recovery, sustained pressure), 7 ✅, 8 ✅, 9 ✅, 10 ✅.

## Follow-Up

None for this run.
