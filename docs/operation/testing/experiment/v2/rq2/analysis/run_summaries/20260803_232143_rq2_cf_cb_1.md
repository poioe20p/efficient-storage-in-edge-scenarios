# Run Summary - 20260803_232143

## Conclusion

Correctly-aligned arm (fixed_compute_first × compute-bound): compute scaled to the per-tier budget cap (4/LAN), the compute bottleneck was relieved with excellent episode service quality (p95 0.25 s, p99 0.50 s, 0.44 % failures), and scale-down reclaimed the dynamic compute nodes. Run matches the plan's expectation for this cell.

## Main Points

- Episode induction valid: bottleneck validation PASS on both LANs — `median(avg_time_proc_ms)` 0.47 / 0.60 ms vs `db_ms` 0.0 (compute-bound confirmed, criterion 2); compute CPU 41 % / 52 %.
- Fixed-arm suppression holds (criterion 3): 8 compute actions, **0 storage**; `selected_action="storage"` never emitted. Budget binds at 4/tier/LAN (criterion 5): `lan1 c=4 s=0`, `lan2 c=4 s=0` — compute budget exhausted exactly at cap.
- Scale-up + scale-down both exercised (criterion 7): 8 dynamic compute containers (4/LAN) spawned 23:23–23:27, removed 23:29–23:37; all cleanups DONE (net_cleanup 0.37–0.85 s); T9.8 fire-keyed scale-down check OK.
- Data path fixed: storage secondaries served reads with `secondaryPreferred` + pool 6 + flows 1 (campaign-validated, 0× NotPrimaryOrSecondary).
- Caveat: relief analysis reports **0/8** in-tier `score_norm` recoveries (criterion 6 metric-level divergence) despite excellent service quality — see Practical Interpretation.

## Evidence

### Elasticity Events

- 8 `edge_server_{lan1,lan2}_dyn1..4` (compute) spawned during `compute_bound_episode`; no dynamic storage.
- Scale-up anchors: spawn→online ≈ 0.9–1.18 s (docker_run + net_attach), READY via `vip_backend_registered`.
- Scale-down occurred: dyn nodes removed across 23:29–23:37 (episode tail → demand_drop); all `cleanup_done`, no controller errors.
- Time-to-usable-capacity (compute, `--anchor decision`): TTFT median **39.45 s**, TFR median **33.85 s** (per-spawn TTFT 19.0–129.3 s); negative `init_time_s` is a known tooling artifact (per_node_stats window granularity lags HTTP TFR) — not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers in `container_events.csv`; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `compute_bound_episode`: `server_count` mean 1.94, **max 4.0**; `storage_count` mean 0.17, max 1.0 (static only — storage never scaled, as expected for compute-first).
- `demand_drop` / `recovery_gap`: server back to ~1.0–1.08 (dynamic compute reclaimed); storage ~0.92–0.98.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 599 | 1132 | 3/235 (1.28 %) |
| compute_bound_episode | 101 | 254 | 645/145,308 (0.44 %) |
| recovery_gap | 996 | 1524 | 2/92 (2.17 %) |
| demand_drop | 376 | 1106 | 7/1,446 (0.48 %) |

### Traffic Handling

- Episode phase (`service_pressure` mix) dominates: 145,308 of 147,081 recorded requests; p50 0.0068 s, p99 0.503 s — no meaningful timeouts (single 30 s tail, `CURL_MAX_TIME=30`).
- Per-LAN episode failures: lan1 0.36 %, lan2 0.55 % — mild asymmetry, both well below 1 %.
- Node-minutes: compute **125.70** (0.855 per 1,000 requests), storage 0.00.

## Practical Interpretation

- **H1/H2 fit:** the correctly-aligned fixed arm relieves its targeted tier (compute) — service stays healthy at the budget cap. This run anchors the compute-bound reference for the cross-over (criterion 8): cf_cb_1 p95 0.25 s / p99 0.50 s / 0.44 % failures.
- **Criterion 6 nuance:** the relief tool shows 0/8 targeted-tier `score_norm` recoveries (score never crossed back under `compute_threshold` in the measured window), yet latency and failures are excellent. The score-norm metric did not register the recovery; treat this as a metric-level caveat, not a service failure — the episode latency/p99 and scale-down behavior are the load-bearing evidence here.
- Budget/cap separation holds: budget (4) binds at the cap (6) — 4 spawns per LAN are budget-limited, distinguishable from cap-ineligibility.

## Follow-Up

None for this run. Watch whether the 0/8 relief pattern persists in cf_cb_2/_3; if it does, the relief tool's threshold-window needs review for compute-bound runs.
