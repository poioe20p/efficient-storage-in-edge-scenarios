# Run Summary - 20260804_063432

## Conclusion
Correctly-aligned `fixed_compute_first` × compute-bound: compute scaled 8/8 (4 per LAN), budget exhausted at the 4/tier/LAN cap, episode served at 0.53 % failures and p95 0.234 s, then scaled down cleanly — matches plan H1 for the `cf×cb` cell (the `cb` reference arm).

## Main Points
- **Bottleneck validation (G2): PASS both LANs** — compute-bound induced correctly: median `proc_ms` 0.24 / 0.42 ms vs `db_ms` 0.0 ms; CPU 60.6 / 36.1 %; storage CPU 0.0 %.
- **Actions: compute=8, storage=0** — fixed-arm suppression (criterion 3) holds: storage never selected.
- **Budget (criterion 5):** compute 4/4 per LAN = `ACTION_BUDGET_PER_TIER` cap; caps 6/6 not reached. T9.8 fire-keyed scale-down OK.
- **Relief (criterion 6):** 4/8 recovered in the targeted (compute) tier, median 90.0 s.
- **Episode quality:** p50 0.003 s, p95 0.234 s, p99 0.475 s; failures 837/158,796 = 0.53 % (LAN1 0.34 %, LAN2 0.70 %).
- **TTFT:** compute median 29.5 s; TFR median 24.6 s. Negative `init_time_s` (−2.7 to −7.7) is the known tooling artifact (per_node_stats window granularity lags HTTP TFR) — not a defect.
- **Efficiency (criterion 9):** compute node-minutes 136.50 (0.849/1000 req), storage 0.00.
- **Scale-down (criterion 7): occurred** — 8 removals, all DONE (`cleanup_done=8`, 0 FAILED).

## Evidence
### Elasticity Events
- 8 dynamic compute spawns (4/LAN) in `compute_bound_episode` (06:36:32–06:40:08), online ~1 s after spawn. No dynamic storage.
- Scale-down: 8 `node_removing` + 8 `cleanup_done` (last event 06:49:20, demand_drop); all removals DONE.
- No controller errors / no restart mid-run (campaign context, C2).
- Scale-down: **yes** (criterion 7 satisfied, clean).

### Tier 1 Selective Sync
- **Disabled (not exercised in RQ2)** — `SS_ENABLED=0`; no `sel_sync_*` containers.

### Resource Shape
- `resource_summary.csv` — episode: `server_count` mean 1.97 (max 4.0) — scaled from 1 to 3–4; `storage_count` mean 0.17 (max 1.0). Demand_drop: server mean 1.05 (compute drained).
- Bottleneck-validation signals: `T_proc` 0.24 / 0.42 ms, CPU 60.6 / 36.1 %, `T_db` 0.0 ms, storage CPU 0.0 % → compute/CPU pressure only; storage correctly untouched.

### Request Latency by Phase
| Phase | Mean (ms) | Median (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: | ---: |
| baseline | 313 | 6 | 1067 | 1 / 237 (0.42 %) |
| compute_bound_episode | 73 | 3 | 234 | 837 / 158,796 (0.53 %) |
| recovery_gap | 900 | 6 | 1132 | 2 / 94 (2.13 %) |
| demand_drop | 277 | 6 | 1092 | 3 / 1,569 (0.19 %) |

### Traffic Handling
- Episode phase dominates: 158,796 / 160,696 requests (99 %). p99 0.48 s — 94 episode requests ≥ 29.9 s (0.06 %), negligible.
- Per-LAN split: LAN2 failures higher (0.70 % vs 0.34 %) but both < 1 %; 1–2 recovery_gap failures on a small sample (2/94) are noise, not degradation.
- Highest offered demand of the 6 runs (158.8 k episode requests) served at a ~3 ms p50 — compute scale-out absorbed it.

## Practical Interpretation
- **H1 alignment:** the correctly-aligned fixed arm relieves compute; this is the `cb` reference for the cross-over (criterion 8). `ba_cb_3` matched it (p95 0.237 vs 0.234 s; failures 0.15 % vs 0.53 %; compute node-minutes 0.831 vs 0.849/1000 req) — H1 supported.
- **Classifier agreement 25/50 = 50.0 %** for this fixed arm is informational only (H3 is evaluated on `ba` cells; the fixed arm's classifier output is not the decision driver).
- **Criteria:** C1 present; C2 PASS; C3 suppression holds; C5 budget binds 4/4; C6 relief 4/8 @ 90 s; C7 scale-down clean; C9 compute node-minutes 0.849/1000 req (aligned-cell baseline); C10 no restart.
- **Divergence / note:** none. Relief median (90 s) is the slowest of the aligned cells this block — compute recovery took longer than `sf_db_3`'s storage recovery (19.4 s), a cross-tier timing observation for the campaign, not a defect.

## Follow-Up
- None.
