# Run Summary - 20260804_051137

## Conclusion
Correctly-aligned `fixed_storage_first` × data-bound: storage scaled 8/8 (4 per LAN), relieved the data tier (4/8 in-tier recovery, median 19.4 s; episode failure 1.05 %) and then scaled down — matches plan H1/H2 for the `sf×db` cell, with a storage cleanup defect (2 containers kept FAILING removal).

## Main Points
- **Bottleneck validation (G2): PASS both LANs** — data-bound induced correctly: median `db_ms` 245.8 / 276.4 ms vs `proc_ms` 19.3 / 97.9 ms; storage CPU 35.3 / 49.4 % (secondary diagnostic elevated, consistent with data pressure).
- **Actions: compute=0, storage=8** (4 per LAN) — fixed-arm suppression (criterion 3) holds: storage only, compute never selected.
- **Budget binds (criterion 5):** storage 4/4 per LAN = `ACTION_BUDGET_PER_TIER` cap; caps (6/6) never reached. T9.8 fire-keyed scale-down check OK.
- **Relief (criterion 6):** 4/8 recovered in the targeted (storage) tier, median 19.4 s — relief met.
- **Episode quality:** p50 0.50 s, p95 1.86 s, p99 30.0 s (timeout tail); failures 313/29,936 = 1.05 % (LAN1 1.11 %, LAN2 0.99 %).
- **TTFT:** storage median 39.35 s (rs_secondary_ready 25–37 s); TFR n/a for storage (no per-node HTTP anchor). Negative `init_time_s` absent here (storage rows); known tooling artifact where present.
- **Efficiency (criterion 9):** storage node-minutes 108.14 (3.396/1000 req), compute 0.00.
- **Scale-down (criterion 7): occurred** — 4 removals in demand_drop — but `edge_storage_lan1_dyn2` and `edge_storage_lan2_dyn3` removal retries FAILED repeatedly (~30–33 s each, `net_cleanup_s` 0.03–0.11 s), a cleanup defect, not workload.

## Evidence
### Elasticity Events
- 8 dynamic storage spawns (4/LAN), all in `data_bound_episode` (05:12:58–05:19:41): spawn → online ~1 s, `rs_secondary_ready` 24.9–37.4 s. No dynamic compute (server_count stays 1.0).
- Scale-down: `node_removing` 05:23:28–05:26:47 (recovery_gap/demand_drop). `cleanup_done=4`; 8 `node_remove_timing` rows, of which `dyn4` (both LANs) and `dyn1`/`dyn3` completed DONE, while `lan1_dyn2` and `lan2_dyn3` removal retried 5–6× with state FAILED (never `cleanup_done`).
- No controller errors / no restart mid-run (campaign context, C2).
- Scale-down: **yes** (criterion 7 satisfied), with the cleanup caveat above.

### Tier 1 Selective Sync
- **Disabled (not exercised in RQ2)** — `SS_ENABLED=0` in `controller_env_snapshot.env`; no `sel_sync_*` containers in `container_events.csv`.

### Resource Shape
- `resource_summary.csv` — episode: `server_count` mean 0.968 (max 1.0) — no compute scale-out; `storage_count` mean 2.632 (max 5.0) — scaled from 1 to 5. Demand_drop: storage mean 2.87 (4 dynamic kept until scale-down).
- Bottleneck-validation signals during episode: `T_db` 245.8 / 276.4 ms dominates `T_proc` 19.3 / 97.9 ms → the storage (data) tier was the pressured one; storage CPU 35.3 / 49.4 % (> floor 35) sustained.

### Request Latency by Phase
| Phase | Mean (ms) | Median (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: | ---: |
| baseline | 236 | 6 | 1062 | 0 / 235 (0.00 %) |
| data_bound_episode | 945 | 501 | 1865 | 313 / 29,936 (1.05 %) |
| recovery_gap | 665 | 64 | 1369 | 1 / 106 (0.94 %) |
| demand_drop | 309 | 8 | 1111 | 3 / 1,563 (0.19 %) |

### Traffic Handling
- Episode phase dominates: 29,936 / 31,840 requests (94 %). p99 = 30.0 s = the `CURL_MAX_TIME` ceiling → timeout-tail requests present (313 episode failures; 314 requests ≥ 29.9 s — latency-tail count), concentrated in the data-bound episode; recovered after scale-down (demand_drop 0.19 % failures).
- Per-LAN failure split close (LAN1 1.11 %, LAN2 0.99 %) — symmetric data pressure, both LANs relieved by their own storage scale-out (budget is per LAN).

## Practical Interpretation
- **H1 / H2 alignment:** this correctly-aligned cell does what the plan expects — storage scale-out relieves the data tier and the episode recovers (1.05 % failures, storage relief 4/8 @ 19.4 s). It is the `db` reference arm for the cross-over (criterion 8).
- **Criteria:** C1 artifacts present; C2 PASS; C3 suppression holds; C5 budget binds at 4/4 per LAN; C6 relief met; C7 scale-down active (with cleanup defect); C9 storage node-minutes 3.396/1000 req is the aligned-cell baseline for `ba×db` comparison; C10 no restart.
- **Divergence / note:** none in policy behavior. The repeated FAILED removals of two storage containers are a cleanup-defect signal to track across the campaign (other `sf_db` replicates should be checked for the same containers).

## Follow-Up
- Track whether `edge_storage_lan*_dyn2`/`dyn3` FAILED-removal pattern recurs across `sf_db` replicates (cleanup retry bug) — flag in results.md if systematic.
