# Run Summary - 20260804_002502

## Conclusion

Bottleneck-aware under data-bound: storage was scaled to the budget cap and relieved the episode (p99 2.19 s, 0.39 % failures vs 30 s / 1.04 % in the mis-aligned cf_db_1) — but the run **spent the full budget on BOTH tiers** (8 compute + 8 storage). The compute fires are classifier misclassifications (77.4 % agreement, H3-reported), not a clean "classify → storage" outcome.

## Main Points

- Episode induction valid: BV PASS both LANs — `db_ms` 217.1 / 211.1 ms vs `proc_ms` 15.4 / 17.0 ms (data-bound confirmed); storage CPU 37 % / 35 %.
- Budget binds on **both** tiers (criterion 5): `lan1 c=4 s=4`, `lan2 c=4 s=4` — storage reached the cap (relief path) but compute also exhausted its budget (misclassification path).
- Classifier-vs-episode agreement 24/31 = **77.4 %** (H3, denominator = episode windows with both tiers eligible); the ~23 % disagreement drove the 8 compute actions. Misclassifications reported, not hidden.
- Storage scale-out delivered measurable relief: 7/16 in-tier recoveries, median **30.2 s**; episode p50 0.13 s, p95 1.17 s, p99 2.19 s, failures 0.39 %.
- Scale-down active on both tiers (criterion 7); T9.8 fire-keyed check OK.

## Evidence

### Elasticity Events

- 8 dynamic compute + 8 dynamic storage containers (4 each per LAN); storage spawned first (00:26) followed by interleaved compute spawns 00:26–00:33.
- Scale-down occurred on both tiers 00:31–00:43; all cleanups `cleanup_done`. Minor log noise: repeated `node_removing` lines for `edge_server_lan1_dyn5` / `dyn3` (multiple remove signals before final cleanup) — no residual containers.
- Time-to-usable-capacity: compute TTFT median **39.75 s** / TFR 33.2 s; storage TTFT median **29.7 s** (per-spawn 29.3–169.8 s; storage has no TFR — first-serving via per_node_stats only). Negative `init_time_s` is a known tooling artifact.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `data_bound_episode`: `server_count` mean 1.96, max 4.0; `storage_count` mean **3.29, max 5.0** (1 static + 4 dynamic) — storage actually scaled.
- `recovery_gap`: storage mean 3.88 (scale-down still in progress); `demand_drop`: storage mean 2.60, server ~1.07.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 236 | 1087 | 0/236 (0.00 %) |
| data_bound_episode | 398 | 1165 | 225/57,949 (0.39 %) |
| recovery_gap | 463 | 1294 | 0/113 (0.00 %) |
| demand_drop | 300 | 1103 | 4/1,529 (0.26 %) |

### Traffic Handling

- Episode phase dominant (57,949 of 59,827 requests); p99 2.19 s — **no 30 s timeout tail** (rare single 30 s outlier), contrasting cf_db_1's timeout-bound episode.
- Per-LAN episode failures balanced: lan1 0.38 %, lan2 0.39 %.
- Node-minutes: compute 142.68 (2.385/1,000), storage 105.67 (1.766/1,000) — both tiers carried capacity.

## Practical Interpretation

- **H1 partially met:** ba_db_1 did select storage and achieved the data-bound relief the plan predicts — matching sf_db_1's quality (p99 2.19 vs 2.77 s) and beating cf_db_1 (30 s).
- **H3 flagged, not hidden:** the 8 compute actions in a data-bound episode are classifier disagreements (finite margin); the run did not select storage *exclusively*. This matters for the efficiency story (node-minutes on both tiers) and should be carried into the cross-cell comparison as a divergence from the "classify → storage" expectation.
- Criterion 8 cross-over holds at the service-quality level; the resource-efficiency half of H2 (wasted actions) is only partially clean here because ba spent compute budget too.

## Follow-Up

Track ba_db_2/_3: if the 7-window compute disagreement (77.4 % agreement) persists across replicates, the compute-fire trigger in data-bound episodes needs a margin review before the cross-cell comparison is finalized.
