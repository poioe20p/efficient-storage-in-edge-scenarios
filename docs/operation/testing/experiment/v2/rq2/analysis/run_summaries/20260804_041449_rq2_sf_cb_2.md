# Run Summary - 20260804_041449

## Conclusion

Mis-aligned `fixed_storage_first` under a compute-bound episode: the compute tier (the actual bottleneck) never scaled (`server_count` stuck at 1), the single storage action produced **no relief (0/1)**, and the episode ran degraded (p50 ≈ 167 ms vs ≈ 2 ms in correctly-aligned compute cells) — H2's wrong-action cost reproduced, with a small wasted budget (1 storage action on lan2).

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_proc 0.72 / 0.82 ms vs T_db 0.00 ms; compute CPU 52.9 / 61.5 %) — episode genuinely compute-bound (criterion 2).
- 1 action, storage only (**compute=0**, `fixed_storage_first` never selects compute — criterion 3 met). Budget: lan1 c=0 s=0, lan2 c=0 s=1 — the 1 storage action is the wasted spend on the wrong tier (H2).
- Classifier-vs-episode agreement 51/72 (70.8%) reported for completeness (fixed arm; the classifier saw the compute bottleneck but the gate suppressed the compute action).
- **T9.8 fire-keyed scale-down OK**; no scale-down ran because only 1 storage node was added and it was not removed — absence of scale-down is the expected mis-aligned-cell behavior (criterion 7, not failed).
- Relief: **0/1** actions recovered in-tier — no storage relief, and no compute relief possible (no compute actions).
- Episode service quality: mean 259 ms, p50 167 ms, p95 383 ms, p99 873 ms; 265 failures / 92 024 requests (0.29%).
- Node-minutes: compute **0.00**, storage **3.51** (0.037 per 1000 recorded requests; 93 944 recorded) — the storage node-minutes are the wasted action.
- TTFT storage median 39.1 s (no compute spawns).

## Evidence

### Elasticity Events

- Single dynamic container: `edge_storage_lan2_dyn1`, spawned at 04:31:41 (demand_drop), ready via `rs_secondary_ready` after ≈31 s; it remained up at end of run (no removal).
- No dynamic compute containers at all — the compute-bound episode was served by the static `edge_server_n1/n2` alone.
- Scale-down: none (nothing removed) — consistent with criterion 7's mis-aligned-cell expectation.
- No controller errors/alerts in parsed elasticity events; no controller restart mid-run (C2).

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `server_count` constant mean 1.0 / max 1.0 (no compute scale-out); `storage_count` mean 0.18 / max 1.0 (storage not scaled during the episode; the single storage node came in demand_drop).
- Bottleneck signals (episode): T_proc 0.72 / 0.82 ms vs T_db 0.00 ms, compute CPU 52.9 / 61.5 % — compute CPU is the pressure; storage CPU 0 % (nothing serving data pressure).
- Env knobs: `SCALEUP_POLICY=fixed_storage_first`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 207 | 1051 | 0 (0.00%) |
| compute_bound_episode | 259 | 383 | 265 (0.29%) |
| recovery_gap | 222 | 1105 | 0 (0.00%) |
| demand_drop | 284 | 1085 | 3 (0.19%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 0.29% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (92 024 of 93 944 requests; 100% `service_pressure` at 8 req/s/client). Per-LAN episode failures: lan1 0.31%, lan2 0.27% — balanced.
- No 30 s timeout tail (p99 873 ms). Degradation shows as elevated median latency (167 ms vs 2–4 ms in the correctly-aligned `cf`/`ba` compute cells), not as failures.

## Practical Interpretation

- **H2 (wrong-action cost):** reproduced — the mis-aligned `sf` arm wasted its only action on storage (0/1 relief, 3.51 storage node-minutes), never scaled compute, and ran the whole episode degraded. The degradation is latency-shaped (p50 167 ms, p95 383 ms) rather than failure-shaped because the static compute tier absorbed the 8 req/s/client load at high CPU (53–62%).
- **H1/H3 contrast:** `ba_cb_2` and `cf_cb_2` in this block added compute and held p50 ≈ 2–4 ms / p95 ≈ 175–244 ms — this run is the mis-aligned contrast for both.
- Criteria: 2 ✅, 3 ✅, 5 ✅ (budget under cap — only 1 action), 6 ❌ (0/1 relief, service degraded), 7 ✅ (no scale-down expected in this cell), 8 ✅ (sf worse than cf/ba on compute-bound latency), 9 ✅ (minimal node-minutes but zero productive capacity), 10 ✅.

## Follow-Up

None for this run.
