# Run Summary - 20260804_012235

## Conclusion

Bottleneck-aware under compute-bound: classified correctly **end-to-end** — compute=8, storage=0 — compute scaled to the budget cap with the campaign's best episode p95 (0.222 s) and scale-down active. The per-window classifier agreement is only 54.2 %, so the clean outcome rides on action selection, not on per-window classification perfection.

## Main Points

- Episode induction valid: BV PASS both LANs — `proc_ms` 0.32 / 0.31 ms, `db_ms` 0.0 (compute-bound confirmed); compute CPU 61 % / 58 %.
- Bottleneck-aware selected **compute only** (criterion 4 met at the outcome level): 8 compute actions, 0 storage; budget exhausted on compute at cap (`lan1 c=4 s=0`, `lan2 c=4 s=0`, criterion 5).
- Classifier-vs-episode agreement 26/48 = **54.2 %** (H3, dual-eligible window denominator) — just above chance per-window, yet **no** compute-bound storage fires materialized (storage=0). The finite-margin misclassifications did not translate into wrong-tier spawns.
- Service quality ≈ aligned cf_cb_1: episode p50 0.0029 s, p95 0.222 s, p99 0.757 s; failures 0.66 % (slightly higher than cf_cb_1's 0.44 %).
- Scale-down active (criterion 7): all 8 compute nodes reclaimed 01:30–01:37; T9.8 fire-keyed check OK.

## Evidence

### Elasticity Events

- 8 dynamic compute containers (4/LAN) spawned 01:24–01:27 during `compute_bound_episode`; no dynamic storage.
- Scale-down occurred: removals 01:30–01:37 (episode tail → recovery_gap/demand_drop); all `cleanup_done`, no controller errors.
- Time-to-usable-capacity (compute): TTFT median **29.1 s**, TFR median **23.15 s** (per-spawn TTFT 19.3–39.0 s); negative `init_time_s` is a known tooling artifact.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `compute_bound_episode`: `server_count` mean 1.93, max 3.0 (resource-sampling max below the 8 spawns/4-per-LAN seen in container events — sampling-granularity limitation); `storage_count` mean 0.18, max 1.0.
- `demand_drop` / `recovery_gap`: server back to ~1.06–1.25 (reclaimed); storage ~0.98–1.0.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 200 | 1098 | 0/235 (0.00 %) |
| compute_bound_episode | 73 | 222 | 1,017/154,696 (0.66 %) |
| recovery_gap | 289 | 1132 | 0/121 (0.00 %) |
| demand_drop | 323 | 1080 | 5/1,514 (0.33 %) |

### Traffic Handling

- Episode phase dominant (154,696 of 156,566 requests — highest completed demand of Block 1); p99 0.757 s, no timeout-bound episode.
- **Per-LAN episode failure asymmetry:** lan1 0.08 % vs lan2 1.16 % (~14×) — the only Block-1 run with a strong per-LAN failure split; both LANs had 4 compute spawns, so the imbalance is routing/load-balance related, not scale-out related.
- Node-minutes: compute **130.08** (0.831/1,000 — in line with cf_cb_1's 0.855), storage 0.00.

## Practical Interpretation

- **H1 met:** ba_cb_1 selects compute and achieves relief comparable to the correctly-aligned fixed arm (cf_cb_1) — same scale-out (8/0), same node-minute efficiency (~0.83 vs 0.86 per 1,000), slightly better p50/p95.
- **H3 nuance:** 54.2 % per-window agreement is low, yet the *selected-action* outcome is pure compute. The comparison story should separate "per-window classification" from "end-to-end action choice" — ba delivered the right tier here.
- Criterion 8: cf ≈ ba beat sf in compute-bound (completed demand and latency) — this run supports that at the latency/failure level except for the lan2 failure spike (1.16 %), which should be watched in ba_cb_2/_3.

## Follow-Up

Investigate the lan2 failure asymmetry (0.08 % vs 1.16 %) — check whether it is a routing/load-balance artifact of this replicate before the cross-cell comparison treats ba_cb latency as homogeneous across LANs.
