# Run Summary - 20260804_024927

## Conclusion

`bottleneck_aware` under a data-bound episode classified data-bound and scaled the storage tier to the 4/LAN budget cap (8 storage actions), with storage scale-down and the best episode failure rate of the data-bound cells — H1 supported. Compute also scaled to its cap (both tiers fired), consistent with the episode's mixed compute+DB pressure.

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_db 189.4 / 120.4 ms vs T_proc 14.7 / 15.4 ms; storage CPU 31.6 / 35.9 %) — episode genuinely data-bound (criterion 2).
- 16 actions: **8 compute + 8 storage** (4/LAN each). Budget at cap on both tiers and both LANs (lan1/2 c=4 s=4). No `budget_exhausted` reason (no 5th fire) — budget binds by usage==cap (criterion 5).
- Classifier-vs-episode agreement **26/35 (74.3%)** over episode windows where both tiers were eligible — above chance (H3).
- **T9.8 fire-keyed scale-down OK**; scale-down ran for both tiers — compute reclaimed mid-episode (5 removals during the episode as compute pressure subsided), storage reclaimed in recovery_gap + demand_drop — criterion 7 met.
- Relief: 7/16 actions recovered in-tier, median recovery 40.2 s (criterion 6).
- Episode service quality: mean 527 ms, p50 247 ms, p95 1313 ms, p99 2519 ms; 264 failures / 47 723 requests (0.55%) — best failure rate of the data-bound runs in this block.
- Node-minutes: compute **139.41**, storage **101.13** (2.817 / 2.044 per 1000 recorded requests; 49 479 recorded).
- TTFT median: compute 39.1 s, storage 39.3 s; TFR compute 30.1 s.

## Evidence

### Elasticity Events

- 16 dynamic containers: 8 storage (`edge_storage_*_dyn1,4,6,7,8`) + 8 compute (`edge_server_*_dyn2,3,5,6,7`), interleaved through the episode; storage ready via `rs_secondary_ready` (21–38 s), compute via `vip_backend_registered` (<2 s).
- Scale-down (criterion 7): 5 compute removals **during the episode** (lan1/lan2 dyn2, dyn5; lan1 dyn3) as compute pressure dropped, then storage removals (dyn8 in recovery_gap, dyn7/dyn6 in demand_drop) and remaining compute in demand_drop — all `DONE`.
- No controller errors/alerts in parsed elasticity events; no controller restart mid-run (C2).
- `rq2_spawn_metrics.csv`: storage TTFT outliers on lan2 (dyn4 169.5 s, dyn6 179.8 s with `action_to_spawn_s` 140–151 s — late/slow-ready storage); median storage TTFT 39.3 s. Negative `init_time_s` is the known per_node_stats-window anchoring artifact — not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `storage_count` mean 3.18 / max 5.0 (storage scaled 1→5/LAN); `server_count` mean 1.81 / max 3.0 (compute 1→3).
- Bottleneck signals (episode): T_db 189.4 / 120.4 ms vs T_proc 14.7 / 15.4 ms, storage CPU 31.6 / 35.9 % — DB latency and storage CPU dominate.
- Env knobs: `SCALEUP_POLICY=bottleneck_aware`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 241 | 1097 | 0 (0.00%) |
| data_bound_episode | 527 | 1313 | 264 (0.55%) |
| recovery_gap | 407 | 1194 | 0 (0.00%) |
| demand_drop | 386 | 1110 | 8 (0.57%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 0.55% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (47 723 of 49 479 requests; data-access mix `content_lookup` 0.4 / `content_update` 0.3 / `content_aggregate` 0.25 / `feed_ranking` 0.05 at 5 req/s/client). Per-LAN episode failures: lan1 0.48%, lan2 0.64% — balanced.
- No 30 s timeout tail in the episode (p99 2519 ms); the 30.005 s max is a single tail outlier.

## Practical Interpretation

- **H1 (right action):** supported — `bottleneck_aware` selected storage (the pressured tier), reached parity with the aligned `sf` arm on latency, and beat it on failures (0.55% vs 1.19%); it clearly beats the mis-aligned `cf` arm (2.10% failures, see `cf_db_2`).
- **H2 (wrong-action cost):** compute also fired to its cap (8 actions) — the episode carried real compute CPU alongside DB pressure, so compute actions were not purely wasted; storage node-minutes (101.13) are the dominant cost and the productive one.
- **H3 (decision quality):** 74.3% agreement on episode windows, above chance.
- Criteria: 2 ✅, 4 ✅, 5 ✅ (cap reached on both tiers), 6 ⚠️ (7/16 in-tier recovery), 7 ✅, 8 ✅, 9 ⚠️ (both tiers scaled → higher node-minutes than pure-storage arms), 10 ✅.

## Follow-Up

None for this run.
