# Run Summary - 20260803_235532

## Conclusion

Mis-aligned arm (fixed_compute_first × data-bound): the episode is genuinely data-bound (BV PASS), but there is no storage signal path — compute exhausted its budget on the wrong tier, storage never scaled, episode p99 hit the 30 s client timeout and failures rose to 1.04 %. Service stayed degraded (H2 reproduced).

## Main Points

- Episode induction valid: bottleneck validation PASS both LANs — `db_ms` 306.8 / 413.2 ms vs `proc_ms` 36.7 / 108.3 ms (data-bound confirmed, criterion 2); storage CPU 34 % / 37 %.
- Fixed-arm suppression (criterion 3): **8 compute, 0 storage** actions; compute budget exhausted at cap (`lan1 c=4 s=0`, `lan2 c=4 s=0`, criterion 5) on the wrong tier.
- Wrong-action cost (H2): storage (the bottleneck) stayed at 1 static node while compute scaled to 4/LAN; episode p99 = **30.0 s** (timeouts), failures **1.04 %**; node-minutes 4.593/1,000 requests — ~5.4× the aligned cf_cb_1 (0.855/1,000).
- Scale-down still ran on the (wrong-tier) compute nodes (criterion 7); T9.8 fire-keyed check OK.

## Evidence

### Elasticity Events

- 8 dynamic compute containers (4/LAN) spawned 23:57–00:01 during `data_bound_episode`; **no dynamic storage** spawned.
- Scale-down occurred: compute dyn removed 00:02–00:10; all `cleanup_done`, no controller errors.
- Time-to-usable-capacity (compute): TTFT median **59.6 s**, TFR median **55.65 s** (per-spawn TTFT 19.5–149.9 s); negative `init_time_s` is a known tooling artifact, not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `data_bound_episode`: `server_count` mean 1.69, max 4.0 (compute scaled out on the wrong tier); `storage_count` mean 0.87, **max 1.0** — the pressured tier never scaled.
- `demand_drop` / `recovery_gap`: server back to ~1.0; storage ~0.96–0.99.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 226 | 1089 | 0/235 (0.00 %) |
| data_bound_episode | 958 | 2076 | 304/29,284 (1.04 %) |
| recovery_gap | 536 | 1267 | 1/107 (0.94 %) |
| demand_drop | 342 | 1102 | 5/1,508 (0.33 %) |

### Traffic Handling

- Episode phase (`service_pressure`) dominant; **p99 30.0 s** = the `CURL_MAX_TIME=30` timeout — requests were timing out during the episode (max 30.006 s).
- Per-LAN episode failures: lan1 0.99 %, lan2 1.08 % — balanced, both ~1 %.
- Episode p50 0.48 s / p95 2.08 s — much worse than the aligned storage arm (sf_db_1 p50 0.40 s / p95 1.37 s / p99 2.77 s).
- Node-minutes: compute **143.00** (4.593/1,000), storage 0.00; only 31,134 recorded requests (throughput-limited by the degraded data path).

## Practical Interpretation

- **H2 (wrong action cost) reproduced:** no storage signal path in `fixed_compute_first` → budget spent on compute, storage unrelieved, degradation persists through the episode (timeouts at 30 s, ~1 % failures). This is the data-bound "stays degraded" cell the plan predicts.
- **Cross-over evidence (criterion 8):** p99 30 s (cf_db_1) vs 2.77 s (sf_db_1) and failures 1.04 % vs 0.78 % — the mis-aligned vs aligned contrast the campaign headlines on.
- Relief analysis: 4/8 compute-tier recoveries (median 49.3 s) — but this is relief in the *wrong* tier; the storage bottleneck never got an action, which is the operative failure.

## Follow-Up

None for this run. Confirms the mis-aligned reference for the data-bound cross-over.
