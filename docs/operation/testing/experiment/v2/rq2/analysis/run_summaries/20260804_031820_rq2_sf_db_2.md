# Run Summary - 20260804_031820

## Conclusion

Correctly-aligned `fixed_storage_first` under a data-bound episode scaled only storage (8 actions, 4/LAN at the budget cap), with storage in-tier relief and the expected `compute=0` suppression — H1 reference arm behaves as planned. A post-episode storage-removal cleanup defect (repeated `FAILED` removals) is noted but does not affect workload conclusions.

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_db 244.6 / 182.6 ms vs T_proc 22.0 / 15.8 ms; storage CPU 32.4 / 20.0 %) — episode genuinely data-bound (criterion 2).
- 8 actions, all storage (**compute=0**, `fixed_storage_first` never selects compute — criterion 3 met). Budget at cap (lan1/2 s=4, c=0). No `budget_exhausted` reason (criterion 5 by usage==cap).
- Classifier-vs-episode agreement 31/43 (72.1%) reported for completeness (fixed arm; policy-contaminated, not a gate).
- **T9.8 fire-keyed scale-down OK**; storage scale-down ran in demand_drop — criterion 7 met.
- Relief: 4/8 actions recovered in-tier, median recovery 20.2 s — storage relief achieved (criterion 6).
- Episode service quality: mean 1055 ms, p50 588 ms, p95 1972 ms, p99 30.0 s (timeout tail); 325 failures / 27 253 requests (1.19%).
- Node-minutes: compute **0.00**, storage **106.28** (3.649 per 1000 recorded requests; 29 123 recorded) — storage-only spend.
- TTFT storage median 29.4 s (no compute spawns).

## Evidence

### Elasticity Events

- 8 dynamic storage containers (4/LAN, `edge_storage_*_dyn1..4`), spawned through the episode; ready via `rs_secondary_ready` in 24–37 s.
- Scale-down (criterion 7): storage removals in demand_drop (03:31–03:39). First removals `DONE`; several removals (lan1 dyn2/dyn3, lan2 dyn3) repeat with state **`FAILED`** and `total_s` ≈ 30–54 s (drain/cleanup retries) — a storage-removal cleanup defect in this run, post-workload, not affecting episode service quality.
- No controller errors/alerts in the parsed elasticity events; no controller restart mid-run (C2).
- `rq2_spawn_metrics.csv`: storage TTFT 29–40 s except lan2 dyn4 (159.5 s with `action_to_spawn_s` 130 s — late/slow-ready outlier).

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `storage_count` mean 2.63 / max 5.0 (storage scaled 1→5/LAN); `server_count` stayed mean 0.95 / max 1.0 (no compute scale-out).
- Bottleneck signals (episode): T_db 244.6 / 182.6 ms vs T_proc 22.0 / 15.8 ms, storage CPU 32.4 / 20.0 % — DB latency dominates.
- Env knobs: `SCALEUP_POLICY=fixed_storage_first`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 356 | 1106 | 1 (0.43%) |
| data_bound_episode | 1055 | 1972 | 325 (1.19%) |
| recovery_gap | 634 | 1344 | 1 (0.93%) |
| demand_drop | 321 | 1147 | 4 (0.26%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 1.19% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (27 253 of 29 123 requests; data-access mix at 5 req/s/client). Per-LAN episode failures: lan1 1.25%, lan2 1.14% — balanced.
- Episode p99 ≈ 30 s indicates a 30 s curl-timeout tail (~1% of requests), also reflected in the 1.19% failure rate — residual data-path tail despite storage relief.

## Practical Interpretation

- **H1 reference:** the correctly-aligned `sf` arm selects storage and relieves it (4/8 in-tier, median 20.2 s) — the reference that `bottleneck_aware` must match. `ba_db_2` (this block) beat this arm on failures (0.55% vs 1.19%) and p50 (247 ms vs 588 ms).
- **H2 contrast:** `ba_db_2` and this run both relieve storage; the mis-aligned `cf_db_2` run cannot (no storage signal path) and stays degraded (2.10% failures).
- Criteria: 2 ✅, 3 ✅, 5 ✅, 6 ✅ (4/8 in-tier), 7 ✅ (storage scale-down ran), 8 ✅ (sf ≈ reference for data-bound), 9 ✅ (compute 0 node-minutes — no wasted compute), 10 ✅.
- Cleanup caveat: storage-removal `FAILED` retries (≈30–54 s each) — post-workload defect, isolated to cleanup, not service.

## Follow-Up

Storage scale-down removal retries (`FAILED` state) in `sf_db` runs warrant a check in the cleanup path (drain/leave timing); monitor across the `sf_db` replicates before the campaign close.
