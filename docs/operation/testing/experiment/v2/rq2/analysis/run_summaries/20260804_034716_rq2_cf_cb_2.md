# Run Summary - 20260804_034716

## Conclusion

Correctly-aligned `fixed_compute_first` under a compute-bound episode scaled only compute (8 actions, 4/LAN at the budget cap), with clean scale-down and the best episode latency/failures of this block — H1 reference arm behaves as planned. `compute=0` on storage confirms the fixed-arm suppression (criterion 3).

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_proc 0.40 / 0.33 ms vs T_db 0.00 ms; compute CPU 51.3 / 51.1 %) — episode genuinely compute-bound (criterion 2).
- 8 actions, all compute (**storage=0**, `fixed_compute_first` never selects storage — criterion 3 met). Budget at cap (lan1/2 c=4, s=0). No `budget_exhausted` reason (criterion 5 by usage==cap).
- Classifier-vs-episode agreement 29/55 (52.7%) reported for completeness (fixed arm; policy-contaminated, not a gate).
- **T9.8 fire-keyed scale-down OK**; compute scale-down ran — 2 removals during the episode, 6 in demand_drop — criterion 7 met (reclaims began earlier than the plan's recovery_gap/demand_drop phrasing).
- Relief: 3/8 actions recovered in-tier, median recovery 60.0 s (criterion 6; sustained episode pressure pushes recoveries into recovery_gap/demand_drop).
- Episode service quality: mean 73 ms, p50 2.4 ms, p95 175 ms, p99 754 ms; 376 failures / 151 159 requests (0.25%) — lowest latency of the 6 runs.
- Node-minutes: compute **129.91**, storage **0.00** (0.849 per 1000 recorded requests; 153 013 recorded).
- TTFT compute median 39.6 s; TFR compute 31.8 s.

## Evidence

### Elasticity Events

- 8 dynamic compute containers (4/LAN, `edge_server_*_dyn1..4`), spawned during the episode; add→ready <1.3 s via `vip_backend_registered`. Lan2 dyn2–dyn4 show `action_to_spawn_s` 70–80 s (spawn latency to first serving: TTFT 89.9–109.3 s) — the only noticeable spawn-queueing in this run.
- Scale-down (criterion 7): compute removals 03:54–04:02 — `lan1_dyn1` and `lan2_dyn2` removed **during the episode**, 6 in demand_drop; all `DONE` with clean cleanup.
- No controller errors/alerts in parsed elasticity events; no controller restart mid-run (C2).
- `rq2_spawn_metrics.csv`: compute TTFT 19–40 s (lan1) / 40–109 s (lan2); negative `init_time_s` is the known per_node_stats-window anchoring artifact — not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `server_count` mean 2.11 / max 4.0 (compute scaled 1→4/LAN); `storage_count` mean 0.18 / max 1.0 (no storage scale-out).
- Bottleneck signals (episode): T_proc 0.40 / 0.33 ms vs T_db 0.00 ms, compute CPU 51.3 / 51.1 % — compute CPU is the pressure source; storage CPU 0 %.
- Env knobs: `SCALEUP_POLICY=fixed_compute_first`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 250 | 1089 | 0 (0.00%) |
| compute_bound_episode | 73 | 175 | 376 (0.25%) |
| recovery_gap | 352 | 1162 | 0 (0.00%) |
| demand_drop | 336 | 1092 | 6 (0.40%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 0.25% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (151 159 of 153 013 requests; 100% `service_pressure` at 8 req/s/client). Per-LAN episode failures: lan1 0.45%, lan2 0.06% — lan2 near-zero, lan1 slightly higher.
- No 30 s timeout tail in the episode (p99 754 ms).

## Practical Interpretation

- **H1 reference:** the correctly-aligned `cf` arm selects compute and relieves the compute bottleneck — the reference that `bottleneck_aware` must match. `ba_cb_2` (this block) matched on service quality (p50 3.7 ms vs 2.4 ms, p95 244 ms vs 175 ms, 0.64% vs 0.25% failures).
- **H2 contrast:** the mis-aligned `sf` arm under the same episode (`sf_cb_2`) cannot add compute and shows the degradation this arm avoids.
- Criteria: 2 ✅, 3 ✅, 5 ✅, 6 ⚠️ (3/8 in-tier), 7 ✅, 8 ✅, 9 ✅ (storage 0 node-minutes — no wasted storage), 10 ✅.

## Follow-Up

None for this run.
