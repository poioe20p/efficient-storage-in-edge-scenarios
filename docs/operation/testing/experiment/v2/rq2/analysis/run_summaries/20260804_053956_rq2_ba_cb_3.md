# Run Summary - 20260804_053956

## Conclusion
`bottleneck_aware` × compute-bound classified compute and selected the compute tier (8 compute, 1 residual storage fire), exhausting the compute budget 4/4 per LAN — matches plan H1 for the `ba×cb` cell; episode service quality stayed excellent (0.15 % failures).

## Main Points
- **Bottleneck validation (G2): PASS both LANs** — compute-bound induced correctly: median `proc_ms` 0.41 / 0.19 ms vs `db_ms` 0.0 ms; CPU 49.0 / 71.8 %; storage CPU 0.0 %.
- **Actions: compute=8, storage=1** — classifier-selected compute (the pressured tier); the single storage spawn (LAN2) is the accepted residual latency-tail storage fire (storage CPU floor-35 regime; appears post-episode).
- **Budget (criterion 5):** compute 4/4 per LAN = cap (lan2 also 1 storage); caps 6/6 not reached. T9.8 fire-keyed scale-down OK.
- **Classifier-vs-episode agreement (H3):** 30/51 = 58.8 % (episode windows, both tiers eligible) — above chance; disagreement finite-margin, per pre-registration.
- **Relief (criterion 6):** 0/9 recovered in-tier, no median recovery time — no score-norm fallback under threshold was recorded; episode latency/failures stayed low regardless (see caveat in Practical Interpretation).
- **Episode quality:** p50 0.0034 s, p95 0.237 s, p99 0.694 s; failures 239/155,582 = 0.15 % (LAN1 0.21 %, LAN2 0.10 %).
- **TTFT:** compute median 34.35 s; TFR median 25.25 s; storage TTFT 39.1 s. Negative `init_time_s` (−8 to −10) is the known tooling artifact (per_node_stats window granularity lags HTTP TFR) — not a defect.
- **Efficiency (criterion 9):** compute node-minutes 130.86 (0.831/1000 req), storage 8.17 (0.052/1000 req).
- **Scale-down (criterion 7): occurred** — 8 removals, all DONE (`cleanup_done=8`, 0 FAILED).

## Evidence
### Elasticity Events
- 9 dynamic spawns: `edge_server_lan*_dyn1..4` (compute, 4/LAN) spawned in the first ~3.5 min of `compute_bound_episode` (05:41:28–05:45:00), online ~1 s after spawn; plus `edge_storage_lan2_dyn5` spawned 05:52:10 (post-episode, recovery_gap) — the residual storage fire.
- Scale-down: 8 compute removals completed DONE (last event 05:54:20, demand_drop). No removal for the single storage node before run end.
- No controller errors / no restart mid-run (campaign context, C2).
- Scale-down: **yes** (criterion 7 satisfied, clean).

### Tier 1 Selective Sync
- **Disabled (not exercised in RQ2)** — `SS_ENABLED=0`; no `sel_sync_*` containers.

### Resource Shape
- `resource_summary.csv` — episode: `server_count` mean 2.05 (max 3.0) — scaled from 1 to 3; `storage_count` mean 0.17 (max 1.0) — the single residual storage node. Demand_drop: server mean 1.05, storage mean 1.48 (compute drained to 1).
- Bottleneck-validation signals: `T_proc` 0.41 / 0.19 ms with CPU 49 / 71.8 % → compute (CPU) pressure with no DB pressure; storage CPU 0.0 % explains why storage rarely fired.

### Request Latency by Phase
| Phase | Mean (ms) | Median (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: | ---: |
| baseline | 243 | 6 | 1088 | 0 / 236 (0.00 %) |
| compute_bound_episode | 79 | 3 | 237 | 239 / 155,582 (0.15 %) |
| recovery_gap | 347 | 6 | 1111 | 0 / 120 (0.00 %) |
| demand_drop | 318 | 6 | 1085 | 5 / 1,513 (0.33 %) |

### Traffic Handling
- Episode phase dominates: 155,582 / 157,451 requests (99 %). p99 0.69 s — no timeout tail in the episode (82 requests ≥ 29.9 s, 0.05 %).
- Per-LAN split: LAN2 slightly better (0.10 % vs 0.21 % failures) — both LANs served by their own compute scale-out.

## Practical Interpretation
- **H1 alignment:** `ba` selected the pressured (compute) tier from telemetry, achieving the same resource shape and latency as the correctly-aligned `cf×cb` arm (p95 0.237 vs 0.234 s in `cf_cb_3`) — the telemetry-driven action matches the right fixed arm.
- **Caveat on relief 0/9:** unlike `cf_cb_3` (4/8 @ 90 s), the relief tool recorded no score-norm fallback for `ba_cb_3`. With all compute actions in the first 3.5 min and p50 0.003 s, this reads as relief happening within the decision window / score normalization not dipping under threshold, not as "no relief". The service-level evidence (0.15 % failures) supports recovery; treat the relief metric for this cb replicate as inconclusive, not negative.
- **Criteria:** C1 present; C2 PASS; C4 ba selects compute (plus 1 accepted residual storage fire); C5 budget binds 4/4; C7 scale-down clean; C8 `ba≈cf` on latency; C9 compute node-minutes 0.831/1000 req in line with `cf_cb_3` (0.849); C10 no restart.
- **H2 note (comparison):** the mis-aligned `sf×cb` arm adds no compute — this run shows what the correct action buys (server_count 2.05, failures 0.15 % vs sf_cb 0.24 %).

## Follow-Up
- None.
