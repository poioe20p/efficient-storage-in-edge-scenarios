# Run Summary - 20260804_015012

## Conclusion

Correctly-aligned arm (fixed_storage_first × data-bound): storage scaled to the 4/LAN action-budget cap (total storage count 5 incl. the static node), the data-bound bottleneck was relieved, episode p99 stayed at 2.77 s (vs 30 s in the mis-aligned cf_db_1), failures 0.78 %, and scale-down reclaimed the storage nodes. Run matches the plan's expectation for this cell and anchors the data-bound reference for the cross-over.

## Main Points

- Episode induction valid: BV PASS both LANs — `db_ms` 103.1 / 136.4 ms vs `proc_ms` 10.8 / 13.9 ms (data-bound confirmed); storage CPU 28 % / 28 %.
- Fixed-arm suppression (criterion 3): **0 compute, 8 storage** actions; storage budget exhausted exactly at the 4/LAN cap (`lan1 c=0 s=4`, `lan2 c=0 s=4`, criterion 5).
- Storage scale-out delivered measurable relief (criterion 6): 4/8 in-tier recoveries, median **20.2 s**; episode p50 0.40 s, p95 1.37 s, p99 2.77 s — no timeout-bound episode.
- Headline contrast (criterion 8): sf_db_1 p99 2.77 s / failures 0.78 % vs cf_db_1 p99 30 s / 1.04 % on the same data-bound episode.
- Scale-down active (criterion 7): all 8 storage nodes reclaimed 02:02–02:05; T9.8 fire-keyed check OK.

## Evidence

### Elasticity Events

- 8 dynamic storage containers (4/LAN) spawned 01:50–01:59 during `data_bound_episode`; no dynamic compute.
- Scale-down occurred: storage dyn removed 02:02–02:05 (demand_drop); all `cleanup_done` (net_cleanup 0.6–1.2 s), no controller errors.
- Time-to-usable-capacity (storage): TTFT median **34.65 s** (per-spawn 29.1–159.3 s); storage has no TFR (first-serving via per_node_stats window only).

### Tier 1 Selective Sync

Disabled (not exercised in RQ2). No `sel_sync_*` containers; Tier 1 selective-sync, persistent reserve, and cross-region are disabled in this campaign.

### Resource Shape

- `data_bound_episode`: `server_count` mean 0.95, max 1.0 (compute untouched — correct); `storage_count` mean **3.26, max 5.0** (1 static + 4 dynamic).
- `recovery_gap`: storage mean 3.38 (scale-down in progress); `demand_drop`: storage mean 2.87 → reclaimed toward baseline.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 337 | 1083 | 1/236 (0.42 %) |
| data_bound_episode | 715 | 1369 | 301/38,627 (0.78 %) |
| recovery_gap | 574 | 1303 | 1/106 (0.94 %) |
| demand_drop | 471 | 1099 | 10/1,339 (0.75 %) |

### Traffic Handling

- Episode phase dominant (38,627 of 40,308 requests); p99 2.77 s, max 30.0 s (single tail) — no systematic timeouts, unlike cf_db_1.
- Per-LAN episode failures balanced: lan1 0.77 %, lan2 0.79 %.
- Node-minutes: storage **104.17** (2.584/1,000), compute 0.00.

## Practical Interpretation

- **H1/H2 fit:** the correctly-aligned storage-first arm relieves its targeted tier (storage) and holds the episode healthy at the budget cap — the data-bound reference for the cross-over (p99 2.77 s, 0.78 % failures).
- **Cross-over (criterion 8) reproduced in Block 1:** sf_db_1 ≈ ba_db_1 (p99 2.77 vs 2.19 s, failures 0.78 % vs 0.39 %) and both beat cf_db_1 (30 s / 1.04 %). The data-path fix (secondaryPreferred) makes storage serving measurable and the storage arm's relief real.
- Budget/cap separation holds: 4 spawns per LAN are budget-limited at the cap (6) — budget binds, distinguishable from cap-ineligibility.

## Follow-Up

None for this run.
