# Run Summary - 20260804_044209

## Conclusion

Mis-aligned `fixed_compute_first` under a data-bound episode: with no storage signal path, storage never scaled (`storage_count` stayed at 1), all 8 compute actions (budget at cap) were spent on the wrong tier, and the service stayed degraded — 2.10% episode failures and a 30 s timeout tail, the worst of the 6 runs — H2's "no signal path to storage → stays degraded" reproduced.

## Main Points

- G2 bottleneck validation **PASS** on both LANs (median T_db 276.3 / 268.7 ms vs T_proc 15.4 / 34.5 ms; storage CPU 28.4 / 32.1 %) — episode genuinely data-bound (criterion 2).
- 8 actions, all compute (**storage=0**, `fixed_compute_first` never selects storage — criterion 3 met). Budget at cap (lan1/2 c=4, s=0) — the full budget wasted on the wrong tier (H2). No `budget_exhausted` reason (criterion 5 by usage==cap).
- Classifier-vs-episode agreement 38/53 (71.7%) reported for completeness (fixed arm; the classifier saw the DB bottleneck, but the gate has no storage path).
- **T9.8 fire-keyed scale-down OK**; compute scale-down ran — 5 removals during the episode (compute reclaimed as the data-bound episode's compute pressure subsided), 3 in demand_drop — criterion 7 met.
- Relief: 5/8 actions recovered in-tier (compute score fell back) but **the bottleneck tier (storage) was never scaled and never relieved** — service stays degraded (H2).
- Episode service quality: mean 938 ms, p50 482 ms, p95 1979 ms, p99 30.0 s (timeout tail); 624 failures / 29 689 requests (2.10%) — worst failure rate of the 6 runs.
- Node-minutes: compute **148.98**, storage **0.00** (4.730 per 1000 recorded requests — the highest per-demand cost of the block; 31 497 recorded).
- TTFT compute median 29.4 s; TFR compute 29.1 s.

## Evidence

### Elasticity Events

- 8 dynamic compute containers (4/LAN, `edge_server_*_dyn1..4`), spawned during the episode; add→ready <1 s via `vip_backend_registered`.
- No dynamic storage containers — the data-bound episode was served by the static storage nodes alone (storage never scaled).
- Scale-down (criterion 7): compute removals 04:49–04:57 — 5 **during the episode** (lan1 dyn1/dyn3/dyn4, lan2 dyn1/dyn2), 3 in demand_drop (lan2 dyn3/dyn4, lan1 dyn2); all `DONE`.
- No controller errors/alerts in parsed elasticity events; no controller restart mid-run (C2).
- `rq2_spawn_metrics.csv`: compute TTFT 19–40 s; negative `init_time_s` is the known per_node_stats-window anchoring artifact — not a defect.

### Tier 1 Selective Sync

Disabled (not exercised in RQ2) — `SS_ENABLED=0` in the env snapshot.

### Resource Shape

- Episode: `server_count` mean 1.80 / max 3.0 (compute scaled 1→3/LAN); `storage_count` mean 0.88 / max 1.0 — **no storage scale-out** (stayed at 1).
- Bottleneck signals (episode): T_db 276.3 / 268.7 ms vs T_proc 15.4 / 34.5 ms, storage CPU 28.4 / 32.1 % — DB latency dominates, storage capacity flat.
- Env knobs: `SCALEUP_POLICY=fixed_compute_first`, `ACTION_BUDGET_PER_TIER=4`, `LATENCY_SIGNAL_MODE=median`, `SCALEUP_STORAGE_CPU_FLOOR=35`, `EDGE_MONGO_READ_PREFERENCE=secondaryPreferred`, `EDGE_MONGO_MAX_POOL_SIZE=6`, `VIP_DATA_PER_CONNECTION_FLOWS=1`.

### Request Latency by Phase

| Phase | Mean (ms) | p95 (ms) | Failures |
| --- | ---: | ---: | ---: |
| baseline | 354 | 1107 | 1 (0.43%) |
| data_bound_episode | 938 | 1979 | 624 (2.10%) |
| recovery_gap | 478 | 1256 | 1 (0.82%) |
| demand_drop | 373 | 1100 | 7 (0.48%) |

Latency values converted to ms from `latency_summary.csv` (recorded in seconds). Failures from `client_requests.csv` (`http_status != 200`); episode failure rate 2.10% matches `campaign_dataset.csv` `ep_failure_pct`.

### Traffic Handling

- Episode phase dominates (29 689 of 31 497 requests; data-access mix at 5 req/s/client). Per-LAN episode failures: lan1 2.56%, lan2 1.61% — lan1 worst in the block.
- Episode p99 ≈ 30 s indicates a 30 s curl-timeout tail, consistent with the 2.10% failure rate — the degradation signature of the un-scaled storage tier.

## Practical Interpretation

- **H2 (wrong-action cost):** reproduced — compute-first under data-bound has no signal path to storage, so storage never scaled and the service stayed degraded for the whole 600 s episode (failures 2.10%, p99 30 s) while the full compute budget (8 actions, 148.98 compute node-minutes) was spent unproductively.
- **H1/H3 contrast:** `ba_db_2` (0.55% failures, p50 247 ms) and `sf_db_2` (1.19%, p50 588 ms) relieved the storage tier in this block; this run is the mis-aligned contrast that makes the cross-over (criterion 8) visible for data-bound.
- Criteria: 2 ✅, 3 ✅, 5 ✅ (cap reached on the wrong tier), 6 ❌ (storage — the bottleneck — never relieved; compute-only "in-tier" recoveries are irrelevant to the DB pressure), 7 ✅, 8 ✅ (cf worst on data-bound), 9 ❌ (highest node-minutes per demand on the wrong tier), 10 ✅.

## Follow-Up

None for this run.
