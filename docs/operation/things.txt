# Tier 1 selective sync — defaults

The Tier 1 thresholds are tuned to the standard `phases.json` workload and live in code, not in `osken-controller.env`:

- `SS_ENABLED` defaults to `1` ([`scaling_config.py`](../../source/sdn_controller/scaling_config.py))
- `TAU_DADOS_MS` defaults to `65` ms ([`edge_server/source/app.py`](../../source/docker/edge_server/source/app.py), [`selective_sync/hotness.py`](../../source/sdn_controller/selective_sync/hotness.py)) — sized to peer-LAN p95 measured on the standard `phases.json` workload (~40 ms baseline, ~75 ms under `cross_region_hotspot` with WAN emulation) so the breach gate fires under hotspot load while staying silent in `baseline`/`local_moderate`. **Requires WAN emulation** on the nat-router; default profile `metro` (`WAN_RTT_MS=10 ms`) — see [Topology — WAN Emulation](topology/topology_overview.md#wan-emulation-inter-lan-latency).
- `SS_MIN_READS_PER_WINDOW` defaults to `14` ([`scaling_config.py`](../../source/sdn_controller/scaling_config.py)) — sized to per-window read counts seen in `cross_region_hotspot` / `storage_stress`.

Override via env only for off-spec experiments. WAN profile knobs (`WAN_RTT_MS`, `WAN_JITTER_MS`, `WAN_LOSS_PCT`, `WAN_RATE_KBIT`) live in [`source/scripts/wan.env`](../../source/scripts/wan.env); re-tune without rebuild via [`source/scripts/tools/wan_set.sh`](../../source/scripts/tools/wan_set.sh).

The coordinator-state PUB sockets bind on `5561`/`5562` (set in [`build_network_setup.sh`](../../source/scripts/build_network_setup.sh)); set `COORDINATOR_STATE_PUB_PORT=0` to disable. The collector subscribes to both via `--lan1-coord-pub` / `--lan2-coord-pub` flags ([`run_experiment.sh`](../../source/scripts/testing/run_experiment.sh)).

## Add `SCALEDOWN_COOLDOWN_S` (default 90 s) — suppress scale-down evaluation after a scale-up event to prevent thrashing

Add `NODE_BIRTH_GRACE_S` (default 60 s) — skip absent-node counting for newly spawned nodes during bootstrap/RS-join

Lower `SCALEUP_SCORE_THRESHOLD` from 0.85 → 0.70 — react at moderate degradation instead of severe

Lower `SCALEUP_REQUIRED` from 3 → 2 — reduce reaction time by ~10 s

Investigate failed `scale_down_data` operations (both LANs) — RS removal scripts failed for `edge_storage_lan1_dyn1` and `edge_storage_lan2_dyn2`

## General

 create a script for just reinitializzing the controller with a modified script

check the OpenLoad tool, to measure response time.

check for mongodb articles (try to see for cdn, data distribution, data popularity and data trends) on google scolar and iee.

Ao inves de tentar dar handle da decisao de que storage utilizar para cada pedido que é feito, deixa-se um conjunto de storages possiveis de se utilizar no server container e vai-se atualizando esse mapeamento consoante o estado da rede

Maybe pourposefully lower the mongodb instance storage capability right away in order to see variance in preenchimento

se calhar utilizar o mongodb arbitro para as cenas de aggregação

se calhar para cada rede que o controlador recebe ele assume qual é o aggregator a qque deve estar conectado tambem

quando houver dados ver como utilizar snapshots assim os replicasets começam com dados

The code under is for instead of going directly to a replicaset you can just cache the hot collections.

Migrate edge-server access tracking off the `cached_collection` wrapper to a pymongo `CommandListener` (extend `db_monitor.py`) so applications don't need to import the wrapper.
Extract the aggregation-stage `$match` filter rewriting for non-point-lookup queries (currently only point `_id` lookups are short-circuited to Tier 1).
Investigate `$collStats` / `top`-based consent signals as an additional promotion input (today the signal is per-request access counters piggybacked on the edge telemetry event).
Exercise the cross-LAN `DataAlert` supersede hook once a cross-LAN RS variant of Tier 2 exists (the guard in `main_n*.py` is in place but dormant — see [`selective_sync/selective_sync_overview.md` — Tier 2 supersede hook](selective_sync/selective_sync_overview.md#tier-2-supersede-hook-dormant)).
