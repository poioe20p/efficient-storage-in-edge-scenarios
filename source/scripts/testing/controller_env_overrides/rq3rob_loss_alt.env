# RQ3 robustness fault delta — loss_alt. Merge over the arm env at launch.
# Drops every 2nd app_ready control event per controller (per LAN) in arrival
# order; the aggregate preservation fraction is the pre-registered metric.
READINESS_EVENT_DROP_MODE=alternate
